mixin videoBlock(video = {})
    .videoBlock
        a(href=routes.vodeoDetail(video.id))
            video.videoBlock__thumnail(src=video.videoFile)
            h4.videoBlock__title= video.title
            h6.videoBlock__views= video.views